[tool.ruff]
line-length = 120
target-version = "py311"

[tool.ruff.lint]
select = ["E","F","I"]
ignore = ["E501"]  # allow long lines globally for now
per-file-ignores = { "src/karnai/rules/dsl.py" = ["E402","F401"] }

[tool.ruff.format]